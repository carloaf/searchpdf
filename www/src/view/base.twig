<!DOCTYPE html>
<html>
<head>
    <title>{{ lang.page_title ?? 'Busca' }}</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="{{ url_base }}/{{ favicon }}" rel="shortcut icon" type="image/vnd.microsoft.icon" />
    
    <!-- CSS -->
    <link rel="stylesheet" href="{{ url_base }}/css/bootstrap-5.3.3.min.css">
    <link rel="stylesheet" href="{{ url_base }}/css/fontawesome.min.css?v={{ "now"|date("U") }}">
    
    <!-- Adiciona o cache buster ao nosso style.css -->
    <link rel="stylesheet" href="{{ url_base }}/css/style.css?v={{ "now"|date("U") }}">
    <link rel="stylesheet" href="{{ url_base }}/css/charts.css?v={{ "now"|date("U") }}">
    <link rel="stylesheet" href="{{ url_base }}/css/equal-height.css?v={{ "now"|date("U") }}">
    
    <!-- Fontes do Google para melhorar a tipografia -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
</head>
<body>
    <div id="content">
        {% block content %}{% endblock %}
    </div>
    
    <!-- Assinatura do desenvolvedor (global) -->
    <div class="footer-signature">
        &copy; copyright - Desenv: Augusto
    </div>

    <!-- JavaScript -->
    <script>
        // Define a URL base da aplicação para uso nos scripts
        window.APP_BASE_URL = '{{ url_base }}';
    </script>
    <script src="{{ url_base }}/js/jquery-3.4.1.min.js"></script>
    <script src="{{ url_base }}/js/bootstrap-5.3.3.bundle.min.js"></script>
    <script src="{{ url_base }}/js/chart.min.js"></script>
    
    <!-- Adiciona o cache buster ao nosso script.js e chart-functions.js -->
    <script src="{{ url_base }}/js/script.js?v={{ "now"|date("U") }}"></script>
    <script src="{{ url_base }}/js/chart-functions.js?v={{ "now"|date("U") ~ random() }}"></script>
    <script src="{{ url_base }}/debug-charts.js?v={{ "now"|date("U") }}"></script>
</body>
</html>
</body>
</html>